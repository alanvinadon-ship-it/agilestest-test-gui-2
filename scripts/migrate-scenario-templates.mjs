import mysql from "mysql2/promise";

async function main() {
  const c = await mysql.createConnection(process.env.DATABASE_URL);

  // Create scenario_templates table
  await c.query(`
    CREATE TABLE IF NOT EXISTS scenario_templates (
      id INT AUTO_INCREMENT PRIMARY KEY,
      uid VARCHAR(36) NOT NULL UNIQUE,
      domain ENUM('IMS','5GC','API_REST','VOLTE','DRIVE_TEST','SECURITY','PERFORMANCE') NOT NULL,
      name VARCHAR(255) NOT NULL,
      description TEXT,
      test_type ENUM('VABF','VSR','VABE') NOT NULL DEFAULT 'VABF',
      difficulty ENUM('BEGINNER','INTERMEDIATE','ADVANCED') NOT NULL DEFAULT 'INTERMEDIATE',
      tags JSON,
      steps JSON,
      required_dataset_types JSON,
      artifact_policy JSON,
      kpi_thresholds JSON,
      profile_template JSON,
      is_built_in BOOLEAN NOT NULL DEFAULT TRUE,
      created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
      updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
      INDEX idx_domain (domain),
      INDEX idx_test_type (test_type),
      INDEX idx_difficulty (difficulty)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
  `);
  console.log("✓ scenario_templates table created");

  // Seed built-in templates
  const templates = [
    {
      uid: "tpl-ims-register-01",
      domain: "IMS",
      name: "Enregistrement SIP IMS",
      description: "Scénario de test d'enregistrement SIP sur le cœur IMS. Vérifie la séquence REGISTER → 401 → REGISTER (auth) → 200 OK, puis le maintien de l'enregistrement via re-REGISTER périodique.",
      test_type: "VABF",
      difficulty: "BEGINNER",
      tags: JSON.stringify(["SIP", "REGISTER", "IMS", "authentification"]),
      steps: JSON.stringify([
        { order: 1, action: "SEND", protocol: "SIP", method: "REGISTER", description: "Envoi REGISTER initial (sans credentials)" },
        { order: 2, action: "EXPECT", protocol: "SIP", method: "401", description: "Attendre challenge 401 Unauthorized" },
        { order: 3, action: "SEND", protocol: "SIP", method: "REGISTER", description: "Envoi REGISTER avec digest auth" },
        { order: 4, action: "EXPECT", protocol: "SIP", method: "200", description: "Attendre 200 OK (enregistrement réussi)" },
        { order: 5, action: "WAIT", duration: 30, description: "Attendre 30s pour re-REGISTER" },
        { order: 6, action: "SEND", protocol: "SIP", method: "REGISTER", description: "Re-REGISTER (refresh)" },
        { order: 7, action: "EXPECT", protocol: "SIP", method: "200", description: "Attendre 200 OK (refresh réussi)" },
      ]),
      required_dataset_types: JSON.stringify(["SIP_CREDENTIALS", "IMS_ENDPOINTS"]),
      kpi_thresholds: JSON.stringify({ register_latency_ms: 2000, auth_success_rate: 99.5 }),
      profile_template: JSON.stringify({ protocol: "SIP", transport: "UDP", domain: "ims.example.com" }),
    },
    {
      uid: "tpl-ims-call-02",
      domain: "IMS",
      name: "Appel VoIP SIP (INVITE → BYE)",
      description: "Scénario complet d'appel VoIP via IMS : établissement (INVITE/180/200/ACK), phase média RTP, puis raccrochage (BYE/200). Vérifie la qualité audio via MOS et les KPIs de latence.",
      test_type: "VABF",
      difficulty: "INTERMEDIATE",
      tags: JSON.stringify(["SIP", "INVITE", "VoIP", "RTP", "MOS"]),
      steps: JSON.stringify([
        { order: 1, action: "SEND", protocol: "SIP", method: "INVITE", description: "Envoi INVITE avec SDP" },
        { order: 2, action: "EXPECT", protocol: "SIP", method: "100", description: "Attendre 100 Trying" },
        { order: 3, action: "EXPECT", protocol: "SIP", method: "180", description: "Attendre 180 Ringing" },
        { order: 4, action: "EXPECT", protocol: "SIP", method: "200", description: "Attendre 200 OK (appel établi)" },
        { order: 5, action: "SEND", protocol: "SIP", method: "ACK", description: "Envoi ACK" },
        { order: 6, action: "MEDIA", protocol: "RTP", duration: 60, description: "Phase média RTP 60s" },
        { order: 7, action: "SEND", protocol: "SIP", method: "BYE", description: "Envoi BYE (raccrochage)" },
        { order: 8, action: "EXPECT", protocol: "SIP", method: "200", description: "Attendre 200 OK (BYE confirmé)" },
      ]),
      required_dataset_types: JSON.stringify(["SIP_CREDENTIALS", "IMS_ENDPOINTS", "MEDIA_CODEC"]),
      kpi_thresholds: JSON.stringify({ call_setup_ms: 5000, mos_score: 3.5, rtp_jitter_ms: 30, rtp_loss_pct: 1 }),
      profile_template: JSON.stringify({ protocol: "SIP", transport: "UDP", domain: "ims.example.com", codec: "AMR-WB" }),
    },
    {
      uid: "tpl-5gc-pdu-03",
      domain: "5GC",
      name: "Établissement session PDU 5G",
      description: "Scénario de test d'établissement d'une session PDU sur le cœur 5GC. Vérifie l'enregistrement UE, l'authentification, et l'établissement de la session de données via N1/N2.",
      test_type: "VABF",
      difficulty: "INTERMEDIATE",
      tags: JSON.stringify(["5G", "PDU", "NAS", "N1", "N2", "SMF"]),
      steps: JSON.stringify([
        { order: 1, action: "SEND", protocol: "NAS", method: "REGISTRATION_REQUEST", description: "Envoi Registration Request (SUCI)" },
        { order: 2, action: "EXPECT", protocol: "NAS", method: "AUTH_REQUEST", description: "Attendre Authentication Request (5G-AKA)" },
        { order: 3, action: "SEND", protocol: "NAS", method: "AUTH_RESPONSE", description: "Envoi Authentication Response" },
        { order: 4, action: "EXPECT", protocol: "NAS", method: "SECURITY_MODE_COMMAND", description: "Attendre Security Mode Command" },
        { order: 5, action: "SEND", protocol: "NAS", method: "SECURITY_MODE_COMPLETE", description: "Envoi Security Mode Complete" },
        { order: 6, action: "EXPECT", protocol: "NAS", method: "REGISTRATION_ACCEPT", description: "Attendre Registration Accept" },
        { order: 7, action: "SEND", protocol: "NAS", method: "PDU_SESSION_ESTABLISHMENT_REQUEST", description: "Envoi PDU Session Establishment Request" },
        { order: 8, action: "EXPECT", protocol: "NAS", method: "PDU_SESSION_ESTABLISHMENT_ACCEPT", description: "Attendre PDU Session Establishment Accept" },
      ]),
      required_dataset_types: JSON.stringify(["UE_IDENTITY", "5GC_ENDPOINTS", "SLICE_CONFIG"]),
      kpi_thresholds: JSON.stringify({ registration_latency_ms: 3000, pdu_setup_ms: 2000, auth_success_rate: 99.9 }),
      profile_template: JSON.stringify({ protocol: "NAS", networkType: "5G-SA", sliceType: "eMBB" }),
    },
    {
      uid: "tpl-5gc-handover-04",
      domain: "5GC",
      name: "Handover inter-gNB (Xn)",
      description: "Scénario de test de handover entre deux gNB via l'interface Xn. Vérifie la continuité de session PDU pendant le déplacement, le temps d'interruption et la perte de paquets.",
      test_type: "VSR",
      difficulty: "ADVANCED",
      tags: JSON.stringify(["5G", "handover", "Xn", "mobilité", "gNB"]),
      steps: JSON.stringify([
        { order: 1, action: "SETUP", description: "Établir session PDU sur gNB source" },
        { order: 2, action: "MEDIA", protocol: "UDP", duration: 10, description: "Trafic données continu (baseline)" },
        { order: 3, action: "TRIGGER", method: "HANDOVER", description: "Déclencher handover vers gNB cible" },
        { order: 4, action: "EXPECT", protocol: "NGAP", method: "HANDOVER_REQUEST", description: "Attendre Handover Request (Xn)" },
        { order: 5, action: "EXPECT", protocol: "NGAP", method: "HANDOVER_REQUEST_ACK", description: "Attendre Handover Request Acknowledge" },
        { order: 6, action: "MEDIA", protocol: "UDP", duration: 10, description: "Trafic données post-handover" },
        { order: 7, action: "ASSERT", description: "Vérifier continuité session + KPIs" },
      ]),
      required_dataset_types: JSON.stringify(["UE_IDENTITY", "5GC_ENDPOINTS", "GNB_CONFIG"]),
      kpi_thresholds: JSON.stringify({ handover_interruption_ms: 50, packet_loss_pct: 0.1, throughput_degradation_pct: 10 }),
      profile_template: JSON.stringify({ protocol: "NAS", networkType: "5G-SA", mobilityPattern: "LINEAR" }),
    },
    {
      uid: "tpl-api-crud-05",
      domain: "API_REST",
      name: "CRUD API REST complet",
      description: "Scénario de test CRUD complet sur une API REST : création (POST), lecture (GET), mise à jour (PUT/PATCH), suppression (DELETE). Vérifie les codes HTTP, les headers, et la cohérence des données.",
      test_type: "VABF",
      difficulty: "BEGINNER",
      tags: JSON.stringify(["REST", "CRUD", "HTTP", "JSON", "API"]),
      steps: JSON.stringify([
        { order: 1, action: "SEND", protocol: "HTTP", method: "POST", path: "/api/resources", description: "Créer une ressource (POST)" },
        { order: 2, action: "ASSERT", status: 201, description: "Vérifier 201 Created + Location header" },
        { order: 3, action: "SEND", protocol: "HTTP", method: "GET", path: "/api/resources/{id}", description: "Lire la ressource (GET)" },
        { order: 4, action: "ASSERT", status: 200, description: "Vérifier 200 OK + body JSON conforme" },
        { order: 5, action: "SEND", protocol: "HTTP", method: "PUT", path: "/api/resources/{id}", description: "Mettre à jour la ressource (PUT)" },
        { order: 6, action: "ASSERT", status: 200, description: "Vérifier 200 OK + données mises à jour" },
        { order: 7, action: "SEND", protocol: "HTTP", method: "DELETE", path: "/api/resources/{id}", description: "Supprimer la ressource (DELETE)" },
        { order: 8, action: "ASSERT", status: 204, description: "Vérifier 204 No Content" },
        { order: 9, action: "SEND", protocol: "HTTP", method: "GET", path: "/api/resources/{id}", description: "Vérifier suppression (GET)" },
        { order: 10, action: "ASSERT", status: 404, description: "Vérifier 404 Not Found" },
      ]),
      required_dataset_types: JSON.stringify(["API_CREDENTIALS", "API_ENDPOINTS"]),
      kpi_thresholds: JSON.stringify({ response_time_ms: 500, error_rate_pct: 0, throughput_rps: 100 }),
      profile_template: JSON.stringify({ protocol: "HTTP", baseUrl: "https://api.example.com", auth: "Bearer" }),
    },
    {
      uid: "tpl-api-perf-06",
      domain: "API_REST",
      name: "Test de charge API (rampe progressive)",
      description: "Scénario de test de performance avec montée en charge progressive : 10 → 50 → 100 → 200 utilisateurs virtuels. Mesure les temps de réponse, le throughput et le taux d'erreur sous charge.",
      test_type: "VSR",
      difficulty: "ADVANCED",
      tags: JSON.stringify(["REST", "performance", "charge", "JMeter", "rampe"]),
      steps: JSON.stringify([
        { order: 1, action: "RAMP", users: 10, duration: 60, description: "Palier 1 : 10 VU pendant 60s (warm-up)" },
        { order: 2, action: "ASSERT", description: "Vérifier baseline : p95 < 200ms, erreurs < 0.1%" },
        { order: 3, action: "RAMP", users: 50, duration: 120, description: "Palier 2 : 50 VU pendant 120s" },
        { order: 4, action: "ASSERT", description: "Vérifier : p95 < 500ms, erreurs < 0.5%" },
        { order: 5, action: "RAMP", users: 100, duration: 120, description: "Palier 3 : 100 VU pendant 120s" },
        { order: 6, action: "ASSERT", description: "Vérifier : p95 < 1000ms, erreurs < 1%" },
        { order: 7, action: "RAMP", users: 200, duration: 120, description: "Palier 4 : 200 VU pendant 120s (stress)" },
        { order: 8, action: "ASSERT", description: "Vérifier : p95 < 2000ms, erreurs < 5%" },
        { order: 9, action: "COOLDOWN", duration: 60, description: "Descente progressive (cooldown)" },
      ]),
      required_dataset_types: JSON.stringify(["API_CREDENTIALS", "API_ENDPOINTS", "TEST_DATA_CSV"]),
      kpi_thresholds: JSON.stringify({ p95_response_ms: 2000, error_rate_pct: 5, throughput_rps: 500 }),
      profile_template: JSON.stringify({ protocol: "HTTP", runner: "jmeter", threads: 200 }),
    },
    {
      uid: "tpl-volte-e2e-07",
      domain: "VOLTE",
      name: "Appel VoLTE end-to-end",
      description: "Scénario de test d'appel VoLTE complet : attachement LTE, établissement bearer dédié, signalisation SIP via IMS, phase média VoLTE, et vérification qualité audio (MOS, jitter, perte).",
      test_type: "VABF",
      difficulty: "ADVANCED",
      tags: JSON.stringify(["VoLTE", "LTE", "IMS", "SIP", "QoS", "bearer"]),
      steps: JSON.stringify([
        { order: 1, action: "SETUP", description: "Attachement LTE + activation bearer par défaut" },
        { order: 2, action: "SEND", protocol: "SIP", method: "INVITE", description: "Envoi INVITE VoLTE (via P-CSCF)" },
        { order: 3, action: "EXPECT", protocol: "DIAMETER", method: "AAR", description: "Attendre AAR (activation bearer dédié QCI=1)" },
        { order: 4, action: "EXPECT", protocol: "SIP", method: "200", description: "Attendre 200 OK (appel établi)" },
        { order: 5, action: "MEDIA", protocol: "RTP", duration: 30, description: "Phase média VoLTE 30s (AMR-WB)" },
        { order: 6, action: "ASSERT", description: "Vérifier MOS ≥ 3.8, jitter < 20ms, perte < 0.5%" },
        { order: 7, action: "SEND", protocol: "SIP", method: "BYE", description: "Raccrochage" },
        { order: 8, action: "EXPECT", protocol: "DIAMETER", method: "STR", description: "Attendre libération bearer dédié" },
      ]),
      required_dataset_types: JSON.stringify(["SIP_CREDENTIALS", "IMS_ENDPOINTS", "LTE_CONFIG", "MEDIA_CODEC"]),
      kpi_thresholds: JSON.stringify({ call_setup_ms: 3000, mos_score: 3.8, rtp_jitter_ms: 20, bearer_setup_ms: 500 }),
      profile_template: JSON.stringify({ protocol: "SIP", transport: "TCP", domain: "ims.volte.example.com", codec: "AMR-WB", qci: 1 }),
    },
    {
      uid: "tpl-security-auth-08",
      domain: "SECURITY",
      name: "Tests d'authentification et autorisation",
      description: "Scénario de tests de sécurité : tentatives d'accès sans token, avec token expiré, avec token d'un autre utilisateur, injection SQL dans les paramètres, et vérification des headers de sécurité.",
      test_type: "VABE",
      difficulty: "INTERMEDIATE",
      tags: JSON.stringify(["sécurité", "auth", "injection", "OWASP", "headers"]),
      steps: JSON.stringify([
        { order: 1, action: "SEND", protocol: "HTTP", method: "GET", description: "Accès sans token → 401" },
        { order: 2, action: "ASSERT", status: 401, description: "Vérifier 401 Unauthorized" },
        { order: 3, action: "SEND", protocol: "HTTP", method: "GET", description: "Accès avec token expiré → 401" },
        { order: 4, action: "ASSERT", status: 401, description: "Vérifier rejet token expiré" },
        { order: 5, action: "SEND", protocol: "HTTP", method: "GET", description: "Accès avec token d'un autre user → 403" },
        { order: 6, action: "ASSERT", status: 403, description: "Vérifier 403 Forbidden (isolation)" },
        { order: 7, action: "SEND", protocol: "HTTP", method: "POST", description: "Injection SQL dans paramètre → rejet" },
        { order: 8, action: "ASSERT", description: "Vérifier pas d'erreur 500 (sanitization)" },
        { order: 9, action: "SEND", protocol: "HTTP", method: "GET", description: "Vérifier headers sécurité (CSP, X-Frame, HSTS)" },
        { order: 10, action: "ASSERT", description: "Vérifier présence headers OWASP" },
      ]),
      required_dataset_types: JSON.stringify(["API_CREDENTIALS", "API_ENDPOINTS", "SECURITY_PAYLOADS"]),
      kpi_thresholds: JSON.stringify({ false_positive_rate: 0, vulnerability_count: 0 }),
      profile_template: JSON.stringify({ protocol: "HTTP", securityLevel: "STRICT" }),
    },
    {
      uid: "tpl-perf-soak-09",
      domain: "PERFORMANCE",
      name: "Test d'endurance (soak test 4h)",
      description: "Scénario de test d'endurance sur 4 heures avec charge constante. Détecte les fuites mémoire, la dégradation progressive des performances, et les problèmes de stabilité long terme.",
      test_type: "VSR",
      difficulty: "ADVANCED",
      tags: JSON.stringify(["performance", "endurance", "soak", "mémoire", "stabilité"]),
      steps: JSON.stringify([
        { order: 1, action: "RAMP", users: 50, duration: 300, description: "Montée progressive à 50 VU (5 min)" },
        { order: 2, action: "SUSTAIN", users: 50, duration: 14400, description: "Charge constante 50 VU pendant 4h" },
        { order: 3, action: "SAMPLE", interval: 300, description: "Échantillonnage KPIs toutes les 5 min" },
        { order: 4, action: "ASSERT", description: "Vérifier : pas de dégradation > 20% vs baseline" },
        { order: 5, action: "ASSERT", description: "Vérifier : mémoire serveur stable (pas de fuite)" },
        { order: 6, action: "COOLDOWN", duration: 300, description: "Descente progressive (5 min)" },
        { order: 7, action: "ASSERT", description: "Vérifier récupération complète post-test" },
      ]),
      required_dataset_types: JSON.stringify(["API_CREDENTIALS", "API_ENDPOINTS", "SERVER_METRICS"]),
      kpi_thresholds: JSON.stringify({ p95_degradation_pct: 20, memory_leak_mb: 50, error_rate_pct: 1 }),
      profile_template: JSON.stringify({ protocol: "HTTP", runner: "k6", duration: "4h" }),
    },
  ];

  for (const tpl of templates) {
    await c.query(
      `INSERT IGNORE INTO scenario_templates (uid, domain, name, description, test_type, difficulty, tags, steps, required_dataset_types, kpi_thresholds, profile_template, is_built_in)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, TRUE)`,
      [tpl.uid, tpl.domain, tpl.name, tpl.description, tpl.test_type, tpl.difficulty, tpl.tags, tpl.steps, tpl.required_dataset_types, tpl.kpi_thresholds, tpl.profile_template]
    );
    console.log(`  ✓ ${tpl.name}`);
  }

  console.log(`✓ ${templates.length} templates seeded`);
  await c.end();
}

main().catch(console.error);
